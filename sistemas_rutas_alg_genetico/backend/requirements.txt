# ──────────────────────────────────────────────────────────────────────────────
# Dependencias — Red Logística Acuícola Real del Meta
# Instalación: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# Servidor web
Flask>=2.0
Flask-CORS>=3.0.10

# Álgebra lineal y optimización
numpy>=1.24
scipy>=1.10          # LP solver (HiGHS via scipy.optimize.linprog)

# Teoría de grafos
networkx>=3.0        # Dijkstra, Bellman-Ford, Flujo Máximo (Edmonds-Karp)
